coverage:
  status:
    project:
      default:
        target: 80.0
        threshold: 0.5
        paths:
          - packages/css/src
    patch:
      default:
        target: 80.0
        threshold: 5.0
        paths:
          - packages/css/src
